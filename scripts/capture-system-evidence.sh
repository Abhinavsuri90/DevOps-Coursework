#!/usr/bin/env bash
set -euo pipefail

# System Evidence Capture Script
# Student: Abhinav

repo_root="$(cd "$(dirname "${BASH_SOURCE[0]}")/.." && pwd)"

echo "System evidence captured for repository at ${repo_root}."
